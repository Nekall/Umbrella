// Styles
import styles from './styles.module.scss';

const Footer = () => {
    return (
        <footer className={styles.__footer}>
            <p>Footer</p>
        </footer>
    )
}

export default Footer;